info:
  name: findCategorieDepense
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/depenses/stats/recap?periode=mensuel
  params:
    - name: periode
      value: mensuel
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
